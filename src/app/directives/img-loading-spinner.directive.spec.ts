import { ImgLoadingSpinnerDirective } from './img-loading-spinner.directive';

describe('ImgLoadingSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLoadingSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
