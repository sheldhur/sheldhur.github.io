import { ImgPreviewDirective } from './img-preview.directive';

describe('ImgPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
